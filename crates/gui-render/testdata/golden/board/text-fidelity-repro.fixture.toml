# Fixture purpose:
#   Verifies hero and tiny curved-glyph fidelity for Datum outline text.

[fixture]
name = "text-fidelity-repro"
lane = "A"
suite = "board-text"
fixture_format_version = 1

[input]
project_path = "crates/engine/testdata/golden/text/native/text-fidelity-repro"
project_kind = "datum-native"

[viewport]
width_px = 1024
height_px = 768
center_mm = [110.0, 75.0]
zoom_mm_per_px = 0.16

[golden]
filename = "text-fidelity-repro.golden.png"
diff_policy = "exact"
diff_tolerance_per_pixel = 0
diff_tolerance_total_px_pct = 0.0
ssim_threshold = 0.0
mask_filename = ""

[blank_check]
expect_non_blank_pct = 1.0
