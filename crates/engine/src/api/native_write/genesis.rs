//! Target home for project-creation (genesis) bootstrap: the remaining
//! `write_canonical_json` project-create sites in
//! `crates/cli/src/command_project_roots.rs` and
//! `crates/cli/src/command_project_support.rs` migrate here so a new
//! project's first shards are born through the substrate, not private writes.
//! No builders live here yet.
