# Phase 2 completion-plan repair

Prepared under WORKFLOW-DELIVERY-IMPLEMENTATION / WDQ-I02.
Repair intake: `dat-wdq-missing-plans-b8y`.
Owning workflow route: `workflow-delivery-rollout`.

This document schedules the missing completion obligations for GUI-P2-CROSSPROBE
and GUI-P2-INSPECTOR. It does not authorize their builds, decide unresolved GUI
mechanisms, claim either lane, or replace their native delivery contracts.
Every new step starts pending; existing dependencies, lifecycle, authorization,
owners and roadmap order remain unchanged. PM025 is the only task selector.

Product authority remains `docs/gui/DATUM_GUI_PHASE_2_SPEC.md` (P2.3/P2.4 and
acceptance/Do NOT sections), PM023's shared interaction backbone, the applicable
S5 selection authority and the controlling Board/Schematic prototypes. C01 must
review the complete actual product routes, prototype DOM/CSS and governing
decisions before authoring its executable contract. No workflow-plan annotation
can amend visual truth; required prototype reconciliation returns to Claude.

The Phase 2 document includes historical partial-landed statements and a legacy
interop fixture. These are not proof of current native prerequisites. C01 must
verify the actual native substrate and fixture closure, record gaps and retain
missing prerequisites as blockers. No import expansion, private selection stack,
authored mutation or new dependency is authorized by this repair.

## Cross-probe — complete P2.3, not two unrelated highlights

<!-- REQ:GUI-P2-CROSSPROBE:XP-C01 -->
### Readiness and contract authorship

After the existing S5A prerequisite, reconcile a complete read-only native
contract for schematic-side object/net selection and highlight, plus cross-pane
projection of the same semantic identity only where it resolves. Include wires,
pins and labels for selected nets; linked-but-distinct component/symbol mappings
remain subordinate related context, never false selected identity. Preserve the
shared S5/PM023 interaction, context envelope and scene-projection backbone.

Review actual P2.1/P2.2 scene, color, focus and camera prerequisites; do not infer
their completion from old prose. Specify deterministic identity/related-context
mapping, absent or stale identity refusal, pane/document/focus/filter boundaries,
multi-selection and unresolved counterparts according to controlling selection
authority. Unresolved mandatory behavior requires its owner decision, not a
runtime guess. Record actual handlers, every exposed native entry surface,
fixture identity, complete build/input/environment closure and source scope.
Answer all eight foundation questions and nine proof dimensions with governed
reasons; this read-only scope cannot mark all normal behavior unavailable.
Record conformance, non-color/accessibility and applicable performance criteria,
real failure/recovery cases and independent-review arrangements. This step
authors readiness evidence, not implementation or final acceptance.

<!-- REQ:GUI-P2-CROSSPROBE:XP-C02 -->
### Exact execution authorization

The owner reviews the complete readiness packet, prerequisites, unresolved
decisions, scope, native fixture/proof matrix, actual handlers and independent
review arrangements. Authorize only that exact reviewed read-only implementation;
missing mandatory behavior or prerequisite remains blocking.

<!-- REQ:GUI-P2-CROSSPROBE:XP-C03 -->
### Authorized implementation

Implement the full reviewed P2.3 contract through shared selection and projection
consumers. Preserve one selected semantic identity, truthful related-context
mapping, schematic highlight roles, per-pane targeting and stale/unresolved
refusal. No authored/journal writes, CLI-string action dispatch, per-editor
interaction fork or unrelated schematic import work. Defining a mapping type or
painting a second highlight does not complete cross-probe behavior.

<!-- REQ:GUI-P2-CROSSPROBE:XP-C04 -->
### Native proof and visual evidence

Exercise real pointer and keyboard selection from Board and Schematic, selected
nets across wires/pins/labels and resolving copper, related-but-distinct subjects,
absent/stale counterparts, multi-selection, pane focus/content changes, filters,
cancel and close/reopen. Prove the full declared matrix against exact native
fixture and build inputs. Compare source/journal bytes and account for new paths;
cross-probe remains consumer state. Prove visible and accessible/non-color roles,
selection projection tests, same-engine goldens and required conformance/resource
gates. Human comparison uses the protected prototypes; never pixel-diff HTML
against wgpu or refresh visual authority to conceal a mismatch.

<!-- REQ:GUI-P2-CROSSPROBE:XP-C05 -->
### Independent replay

A distinct reviewer replays the complete exact native matrix with fresh events
and verifies identity rather than merely matching highlights. Review authority,
input closure, all findings and unchanged source/journal state. Record every
defect with a responsible lane and resolution or explicit owner disposition;
producer self-review and copied logs cannot complete this step.

<!-- REQ:GUI-P2-CROSSPROBE:XP-C06 -->
### Owner acceptance and handoff

Obtain exact owner acceptance of the complete reviewed native P2.3 result and
known-gap dispositions. Hand evidence to the full inspector and native authoring
owners without selecting or authorizing either successor. No partial mapping,
unavailable-only behavior, screenshot alone or planning approval closes this task.

## Full inspector — all P2.4 read-only context surfaces

<!-- REQ:GUI-P2-INSPECTOR:INSP-C01 -->
### Readiness and contract authorship

After the existing cross-probe prerequisite, author the complete P2.4 contract:
component Identity/Placement/Checks sections; schematic Net/Members/Checks (ERC)
inspection; Sheets hierarchy including multi-sheet selection; and schematic
Application Status Bar context (Sheet n/m, Grid, ERC count). The old tracker
summary naming only three component sections does not narrow the governing spec.
Reuse the existing inspector and focused-pane/context/selection authorities;
editable fields and authored operations remain outside this read-only phase.

Reconcile component, net, sheet and focused-document identities, field provenance,
units/formatting, empty/mixed/unavailable and stale results, context changes and
actual ERC finding consumers. Do not accept placeholders, fabricated checks,
conflicting selection identity or implied write controls. Review actual P2.0,
P2.2 and cross-probe prerequisites and complete owning prototype/source routes.
Record exact native handlers, entry surfaces, multi-sheet fixture and build/input
closure, all eight foundation answers, nine proof dimensions, required normal
outcomes, accessibility/non-color and applicable performance criteria. Route
unresolved mandatory field or interaction choices to the owner before readiness;
no design mechanism is ratified by this scheduling document.

<!-- REQ:GUI-P2-INSPECTOR:INSP-C02 -->
### Exact execution authorization

The owner reviews the complete P2.4 readiness, full component/net/sheet/status
coverage, native proof matrix and independent-review arrangements. Authorize
only the exact reviewed read-only implementation after required prerequisites
and decisions; do not silently defer entire surfaces to close a smaller slice.

<!-- REQ:GUI-P2-INSPECTOR:INSP-C03 -->
### Authorized implementation

Implement all reviewed P2.4 consumers on the existing inspector and context
backbone, with model-derived content and correct focused-pane ownership.
Preserve shared units/selection/state authorities. Keep authoring and editable
fields behind their separate write-path authority. A populated component panel
alone is not completion of the net, Sheets and status requirements.

<!-- REQ:GUI-P2-INSPECTOR:INSP-C04 -->
### Native proof and visual evidence

Exercise component and net inspection from both native panes, all named sections,
multi-sheet hierarchy selection and Sheet/Grid/ERC status; switch focus/content
and verify the displayed identity and model-derived results. Include empty,
mixed, unavailable, stale/disappearing subjects, invalid input, cancellation and
close/reopen. Prove readable unit/precision presentation without authored numeric
changes, actual checks rather than invented results, and no source/journal writes.
Cover the complete reviewed fixture/entry-surface matrix, accessibility/non-color,
required performance, structural content tests, same-engine goldens and relevant
conformance gates. Protected prototype comparison is a human review, not a
cross-renderer pixel-diff or permission to bless away discrepancies.

<!-- REQ:GUI-P2-INSPECTOR:INSP-C05 -->
### Independent replay

A distinct reviewer replays the exact full component/net/sheet/status matrix,
including failure and context transitions, with fresh events and verified inputs.
Check semantic field provenance, units, read-only behavior and every visual or
interaction gap. Account for every finding; independent review does not invent
owner acceptance or replace required native proof with structural tests.

<!-- REQ:GUI-P2-INSPECTOR:INSP-C06 -->
### Owner acceptance and handoff

Obtain exact owner acceptance of the full reviewed P2.4 result, prototype
comparison and defect dispositions. Record honest remaining product status;
do not claim authoring or full EDA completion. Closure neither selects nor
authorizes another roadmap item.

<!-- OWNER:GUI-P2-CROSSPROBE:XP-C02:AUTHORIZE -->
<!-- OWNER:GUI-P2-INSPECTOR:INSP-C02:AUTHORIZE -->
## Execution decision rule

Authorize only the named task's exact completed readiness and implementation/
native-proof packet. Revise or defer if mandatory behavior, authority, fixture,
prerequisites or independent review remain unresolved. Approval never waives
dependency/license authority, Claude's prototype lane or the read-only boundary.

<!-- OWNER:GUI-P2-CROSSPROBE:XP-C06:ACCEPT -->
<!-- OWNER:GUI-P2-INSPECTOR:INSP-C06:ACCEPT -->
## Acceptance decision rule

Accept only the named task's exact complete native candidate and independent
replay with every finding dispositioned. Keep required behavior blocking when
unproved. No generic permission to proceed, planning artifact or unrelated
feature approval substitutes for this owner decision.
