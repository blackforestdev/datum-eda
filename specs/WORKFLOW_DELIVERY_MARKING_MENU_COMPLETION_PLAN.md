# Inert marking-menu shell completion

Frontier: GUI-MARKING-MENU. Issue: `dat-marking-menu-shell-g0w`.
Workflow repair: WORKFLOW-DELIVERY-IMPLEMENTATION / WDQ-I02;
`dat-wdq-missing-plans-b8y`. Route: `workflow-delivery-rollout`.

This schedules the existing inert shell, not the finished verb-firing menu or
authoring system. Every checkpoint is pending. Preserve specified/planning
state, existing task order and independent execution authorization. No product
lane is claimed and no operation is authorized by this plan.

Controlling starting points are DATUM_GUI_CONTEXT_MENU_CONTENT.md,
DATUM_GUI_PARAMETRIC_TOOLING.md, the current GUI design specification, PM023,
the menu model and their complete research/prototype routes. The two HTML files
on `prototype-context-and-open-forks` identify themselves as historical studies,
not live decisions. Their presence is not current construction approval. C01
must establish the exact valid visual/interaction authority with the owner and
Claude where reconciliation is needed. Never edit or reactivate those prototypes
from this lane, infer a winning historical alternative or bless a mismatch.

<!-- REQ:GUI-MARKING-MENU:MM-C01 -->
## Reconcile the complete shell and readiness contract

Review all source/consumer routes and the menu-model/registry/icon inventory.
Specify the exact in-scope native surfaces and contextual object, empty-canvas
and multi-selection wheels. Preserve cardinal/secondary/overflow/submenu/icon
rules, stable slots, destructive isolation and selection intersection. Reconcile
the design's general depth limit with the governed three-layer Align hierarchy,
cardinal assignments, delayed display/expert gesture, parent/sub-wheel geometry,
keyboard navigation and narrow/edge placement. Open prototype studies or
contradictory text require explicit owning-lane resolution; do not choose by code
convenience or treat this scheduling plan as the missing GUI contract.

Record actual native shell handlers, entry surfaces, complete fixture/build/input
closure, source scope, eight foundation answers and nine proof dimensions.
Required normal shell behavior must work: opening, contextual presentation,
navigating the reviewed hierarchy/overflow and dismissal with usable focus.
Prove these through their real consumers, not fabricated dispatch keys. Mutation
and not-built leaves remain unavailable with truthful reasons; no typed operation
may be invoked. A refusal-only screenshot cannot prove a usable shell, and
successful shell navigation cannot prove that its disabled EDA verbs work.

Preserve right-button menu ownership and shared pane targeting; no pan fallthrough
or editor-specific interaction fork. Keep existing unrelated supported actions
working; do not disable the application to satisfy this shell's no-operation
scope. Include cancel, stale or missing subject, context changes, capture/focus
loss, accessibility/non-color, conformance and applicable resource criteria.
Resolve mandatory authority/behavior gaps before readiness; the future C01 owner
authors the actual executable delivery contract and native proof matrix.

<!-- REQ:GUI-MARKING-MENU:MM-C02 -->
## Exact owner authorization

Obtain owner approval of the complete shell readiness packet, exact live visual
authority and unresolved-choice dispositions, surface/content matrix, actual
handlers, native proof and independent review arrangements. Authorization is
bounded to the inert shell and cannot enable leaves, author operations, approve
a dependency or claim PM023's complete verb-firing menu requirement.

<!-- OWNER:GUI-MARKING-MENU:MM-C02:AUTHORIZE -->
Authorize only the exact reviewed inert-shell packet after mandatory visual,
interaction and content authority is settled. Historical studies and a green
menu-model inventory are insufficient. Revise or defer unresolved requirements;
keep GUI write-path and actual operation invocation under separate authority.

<!-- REQ:GUI-MARKING-MENU:MM-C03 -->
## Implement the authorized inert shell

Render and navigate the exact menu-model-driven shell through the shared native
interaction and layout/token systems. No hardcoded parallel content inventory,
per-surface menu engine or private mutation path. Preserve stable geometry,
correct contextual content, icons/labels, unavailable reasons and no invocation
of mutating/not-built entries. Do not silently turn this slice into tool wiring,
settings customization or a GUI/prototype redesign.

<!-- REQ:GUI-MARKING-MENU:MM-C04 -->
## Native behavior and nonmutation proof

Exercise the complete reviewed object/empty/multi-selection and surface matrix
through real native input. Show successful opening, labels/icons, cardinal and
secondary selection, submenu/overflow navigation, accelerator behavior and
dismissal; cover every reviewed delayed-display/gesture path without operation
invocation. Include disabled-leaf attempts, mixed/incompatible subjects, stale
context, pane changes, narrow/edge placement and capture/focus loss. Verify
readable non-color states and native accessibility against visible behavior.

Record exact build/fixture/environment/input identities, source and journal
before/after hashes, all new paths, and evidence that no operation or tool-start
escaped. Prove normal editor use continues after cancellation/refusal and clean
close/reopen. Run relevant menu-model/icon, layout/token, conformance, same-engine
golden and resource gates; human comparison uses the exact approved visual
authority, not HTML-versus-wgpu pixel matching. No placeholder, unavailable-only
run or helper test substitutes for native shell behavior.

<!-- REQ:GUI-MARKING-MENU:MM-C05 -->
## Independent replay

A distinct reviewer replays the complete exact shell matrix with fresh events,
matching inputs and no copied producer observations. Verify usable navigation,
disabled-leaf refusal, input ownership, accessibility, scope and nonmutation.
Account for every defect with its responsible lane and explicit disposition.
Independent shell review is not operation capability or owner acceptance.

<!-- REQ:GUI-MARKING-MENU:MM-C06 -->
## Exact owner acceptance and write-path handoff

Obtain exact owner acceptance of the complete inert-shell candidate and its
independent replay with every finding dispositioned. Identify all intentionally
disabled actions and remaining verb/tool authoring obligations. Hand evidence
to GUI-WRITE-PATH without selecting, claiming or authorizing that successor.
Do not report finished marking-menu EDA capabilities or full rollout adoption
from acceptance of this bounded shell.

<!-- OWNER:GUI-MARKING-MENU:MM-C06:ACCEPT -->
Accept only the complete reviewed inert-shell result: actual normal shell
behavior, truthful disabled actions, native nonmutation and independent evidence.
Keep unsupported mandatory shell behavior blocking, and preserve the separate
write-path and product-operation acceptance requirements.
