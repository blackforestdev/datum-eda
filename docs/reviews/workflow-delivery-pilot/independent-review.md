# WDQ-G05 independent review and native replay

Reviewer: `wdq-independent-review-20260906`, actual task
`/root/wdq_independent_review`. Date: 2026-09-06.
Independent of implementation/original producer
`codex-wdq-g01-planning-20260906`.

## Disposition and limits

**Approve the finite pilot's independent replay**, not owner acceptance or live
enforcement. All five mandatory native scenarios and the disabled-pointer Fit
regression were independently executed on the actual reviewed executables.
I inspected all 69 mandatory/regression PNGs myself. All 83 machine observation
checks passed; the separately authored assessment records 19 required-dimension
judgments. This is not a unit-test result substituted for native evidence.

Two nonblocking Console findings remain open. Their `disposition_ref` values in
`review.json` are **prospective locations** for an explicit future owner response
in the governed handoff, not existing or approved deferrals. No owner response
was supplied by this reviewer. `owner_receipt` is null. The exact acceptance
gate must still refuse until authentic owner dispositions and acceptance are
recorded and promoted. This report grants neither.

I did not implement production code, author the original proof, modify a
prototype/golden, change authority, edit tracker/Frontier, or set external trust.
Using the existing capture/package machinery does not reuse producer events or
assessments: all native process runs, timestamps, event streams and observations
in `independent-native/` are new. The reviewed build receipts are intentionally
shared because replay must use the same identified executable/build, not a
different rebuild. Earlier failing producer evidence remains untouched.

## Identified source, build and environment

Production build source: `dde90c754e0a76366a927a0addda60103bd50534`.
Capture-launch repository HEAD: `200ea14227c269ee170d1116a9cff80377130e87`.
Subsequent governance-only HEAD movement is not represented as a new build.

- GUI SHA-256: `2bd1daa4d1a3e8eb393130cd0d543c7f610cfcf3478364827e4e3b807c18f560`.
- CLI SHA-256: `80f8a2b1dc741a60b21bab6e397ff82adc57fcef22672e00d662b5608fdbed4b`.
- Recorded input-manifest blob SHA-256: `54578f6ada63c3f1c65ac8accaef8f5a7e92d93cffc3d202dbbbf75ea565290d`.
- Authority SHA-256: `b598304e6cb13770cf024015eb4963584b0bc948c8280130d5104d812f15f562`.
- Original packet SHA-256, independently recomputed:
  `ab71468a0acd0e845f5e7cd5e2b21aae5121a85dfd5c2d72fa2f5f372c5745a0`.
- Independent replay blob SHA-256:
  `b75b15e40e5f9188814ebaaa4b10b19c6b700c5d9b2cd2b3517fc2624a4c9c1b`.

I independently hashed both executable files and checked each run identity
against the corrected GUI/CLI receipts. I read the guarded successful build log
and both receipts. No Cargo build was run by this reviewer. The command was the
recorded offline, locked, serial guarded build from
`/home/bfadmin/Documents/datum-wdq-source-oy3bxeo8/Cargo.toml`, targeting the
disk-backed shared target and building `datum-gui-app` and `datum-eda-cli` bins.
The receipt identifies rustc 1.93.1, commit
`01f6ddf7588f42ae2d7eb0a2f21d44e8e96674cf`, x86_64-unknown-linux-gnu, LLVM 21.1.8.

I enumerated and hashed all 1,778 files under the ten contract roots in that
source snapshot. The resulting path/hash list exactly equals the recorded
manifest and the same closure from the build revision and current indexed
revision. This is a clean identified build/source claim, not a claim that
unrelated ignored runtime files in the original worktree were absent.

The environment is the actual Linux X11 session generated by private headless
Weston/pixman, rootful Xwayland and private session/accessibility buses. Window
size 1280x768 and scale 1.0 are observed in production states. All inputs use
xdotool XTest, with normal X11 WM_DELETE_WINDOW closure; no desktop window or
inherited desktop bus is used. The independently packaged environment object
equals the original environment exactly. Its reproduction commands use the
same honest `<new-native-root>` placeholders, not copied original output paths.
Actual unique paths and physical inputs are retained in raw run identities,
requested-inputs and inputs logs. No environmental equivalence was invented.

## Relevant-input and authority review

Reviewed the controlling AGENTS/CLAUDE operating rules, G05 handoff, PM041,
gate contract, adoption packet and the workflow-delivery-quality evidence route.
The route's five research sources and five consumers were read in this reserved
independent session; its authority remains unchanged. Before finalizing this
review I also read every complete source and consumer of the second bound
`prototype-editor-shell-and-panes` route: `board-editor.html`,
`schematic-editor.html`, `workspace-panes.html`, `DATUM_GUI_DESIGN_SPEC.md`,
`DATUM_GUI_CONFORMANCE_SPEC.md`, PM019, PM021, and
`PUBLISH_SPACE_VISUAL_STUDY_BRIEF.md` (2,764 lines total). Their focused-pane,
one-model, output-only Console, and one-action-identity rules constrain this
pilot. Historical study badges do not ratify unbuilt schematic/cross-probe,
Publish, selection, or full-shell parity claims; the conformance spec expressly
distinguishes interim first-slice rendering from owner-approved full prototype
parity. No such wider claim is made here. All prototype files remain untouched.
No runtime behavior was used to override a ratified scope or N/A disposition.
`python3 scripts/check_evidence_traceability.py` passed: 20 routes and 114
research/prototype artifacts.

Closure covers every workspace crate, not merely changed menu files: GUI
admission/dispatch, renderer, protocol/registry, pane/camera/focus, accessibility,
Console, engine/model/text, terminal and pinned CLI/verb-registry paths. I
inspected Cargo workspace/path dependencies and include/resource references.
Cargo.toml, Cargo.lock and `.cargo` capture workspace dependencies/build config;
the guarded runner/resource policy capture recorded build flags. Menu/icon JSON
and CSV are explicit roots; embedded font and terminal resources are under
`crates`. The real archive is an explicit root. Runtime paths outside these
roots found in fixture/test helpers or the separate MCP/catalog commands are not
executed by this finite workflow. No production build script or generated-source
exception is hidden by omitting a changed crate. Shared numeric/model code is
included even though numeric authoring is not accepted here.

At review start ignored artifacts under broad `crates` correctly invalidated a
worktree manifest while revision/index/source-snapshot manifests matched. I did
not delete or filter them or narrow roots. The coordinating lane subsequently
reported explicit owner authorization and byte-preserving archival. A current
worktree gate must be checked again before enforcement; no stale-detection
bypass is part of this review.

## Native results

| Scenario | Independently observed result |
| --- | --- |
| S01 | Pointer Fit, editor F and keyboard-row Enter each restore the fitted Board after visible zoom; pad identity/Inspector, other camera, layout and source are retained. Eight images. |
| S02 | Six pointer/keyboard attempts at three missing consumers refuse without invocation; all six complete finite reasons are readable across initial/earlier history captures. Existing F.Cu toggle and pane navigation still work. Eighteen images. |
| S03 | Eligibility follows Board/Schematic focus and Fill Pane; Escape returns editor F. Missing consumers stay unavailable. Disabled-pointer regression preserves schematic focus and enlarged Board until a real canvas click. Thirteen plus four images. |
| S04 | Actual refusals precede usable Fit, two successful normal closes and same-Project reopen; all archive bytes preserved and new sidecars enumerated. Ten images. |
| S05 | Live AT-SPI identities, focus, enabled/sensitive bits, reasons and owned-bus events agree with paint/input. Menu removal retains Terminal; Global Preferences and Terminal coexist; modal dismissal restores menus. Sixteen images, including separate native Preferences window. |

The main-window S05 screenshot while Preferences is open is black above the
terminal strip; the separate native Preferences window is visible and was
inspected. The accepted claim is accessibility-consumer retention and subsequent
menu operability, **not** full Preferences visual, source-health or parity
acceptance. Keyboard-only menubar opening is likewise not claimed: menus are
pointer-opened and row navigation is real keyboard input.

Each mandatory run hashes every original archive member before and after. All
are identical; new paths are limited to the enumerated terminal-context and
tool-session sidecars. No design/journal or preferences writes are accepted.
Compact Console text clips long reasons, which PM033 permits only when full
history remains available; the finite pilot records are visibly complete in the
deliberately opened history and do not overlap. No arbitrary-message claim follows.

## Every dimension and foundation disposition

I reviewed all 45 scenario dimension answers (19 required, 26 N/A), not only the
passing required assertions. The following lists enumerate every N/A:

| Scenario | N/A dimensions and reviewed basis |
| --- | --- |
| S01 | invalid → S02/S03; cancel → S03/S05; precision and undo_redo → no numeric/design authoring; save_reopen → S04; accessibility → S05; failure_recovery → S04. |
| S02 | cancel → S03/S05; precision and undo_redo → no numeric/design authoring; save_reopen → S04; accessibility → S05; failure_recovery → S04. |
| S03 | precision and undo_redo → no numeric/design authoring; save_reopen → S04; accessibility → S05. |
| S04 | cancel → S03/S05; scope → S03; precision and undo_redo → no numeric/design authoring; accessibility → S05. |
| S05 | precision and undo_redo → no numeric/design authoring; save_reopen → S04; failure_recovery → S04. |

Cross-scenario N/A does not remove a dimension from the pilot: its designated
scenario passed actual native replay. Precision N/A concerns authored numeric
values/units, not exemption from checking camera center and zoom. Undo N/A is
not a fabricated Undo pass: every original journal byte remains unchanged.
S04 save_reopen means unchanged-source clean reopening only, never Save,
pending-edit recovery or crash recovery.

All eight foundation answers were reviewed. Required selection_identity is
proved by S01 stable pad and pane identity; undo_cancel by S03/S05 Escape and
unchanged journal; persistence_recovery by S04 normal close/reopen; settings_scope
by focused-context transitions and unchanged configuration in every scenario.
The four N/A answers remain justified: units_precision (no stored geometry/units
change), numeric_entry (no field parsing), grid_snap (no placed/snapped point or
grid setting), library_connectivity (no library, pin/pad or connection authoring).
None is an unresolved requirement relabeled N/A.

## Open findings: independent triage, no owner deferral

`dat-console-scroll-range-4y3` — nonblocking for this finite pilot, still real.
I independently ran an additional same-binary private native diagnostic. After
the six finite refusals, three physical wheel-ups over history produce blank
history (`independent-console-scroll-diagnostic/overscroll-three-up.png`). The
source clamps against theoretical capacity rather than actual records. However,
mandatory S02's initial and one-scroll history captures expose all six complete
records; the bounded action-refusal, no-invocation and source-preservation claims
do not require overscrolling. This is not a claim of general Console correctness.
The subsequent return attempt failed because I passed unsupported negative
wheel repeats to the runner, not because of a product crash. That partial run,
its failure explanation and all raw artifacts are preserved. No successful
recovery/normal close is claimed for this extra diagnostic.

`dat-console-oversized-history-9uz` — nonblocking for this finite pilot, still
real. Read-only inspection of `datum_console/history_layout.rs` confirms a
record taller than the panel is clipped with `height.min(available)`; record-based
scrolling cannot reveal its tail. PM033's full-history requirement remains unmet
for arbitrary oversized messages. None of these finite pilot refusals is such a
record: I visually read them in full in S02/S03/S04. This review neither invents
an oversized native test nor accepts arbitrary long-message/history behavior.

Both findings require explicit owner deferral if the owner chooses acceptance.
The prospective handoff marker in `review.json` must receive authentic exact
`DEFER <issue-id>` lines and promoted authority; this reviewer cannot grant them.

## Reproduction and retained proof

From repository root, for each of `PILOT-S01`, `PILOT-S02`, `PILOT-S03`,
`PILOT-S04`, `PILOT-S05`, and `PILOT-S03-pointer-regression`, I ran:

```text
python3 scripts/workflow_delivery_pilot_capture.py --gui target/debug/datum-gui --cli target/debug/datum-eda --output docs/reviews/workflow-delivery-pilot/independent-native/<scenario> --scenario <scenario>
python3 scripts/workflow_delivery_pilot_observations.py --native-root docs/reviews/workflow-delivery-pilot/independent-native --build-root docs/reviews/workflow-delivery-pilot/corrected-build --output docs/reviews/workflow-delivery-pilot/independent-observations.json
python3 scripts/workflow_delivery_pilot_package.py --native-root docs/reviews/workflow-delivery-pilot/independent-native --build-root docs/reviews/workflow-delivery-pilot/corrected-build --assessment docs/reviews/workflow-delivery-pilot/independent-assessment.json --output docs/reviews/workflow-delivery-pilot/independent-proof-artifacts --proof docs/reviews/workflow-delivery-pilot/independent-replay.json --producer-session wdq-independent-review-20260906
```

Use fresh output paths for reproduction; existing evidence is immutable.
Raw native AT-SPI trailing spaces, event logs, state snapshots and screenshots
are retained. The packager's generic “producer proof” text means the producer of
this separate replay artifact, not the original implementation producer.
The independent assessment is hand-authored from these observations. No golden,
authority digest or original event was replaced to make a check pass.

## Staged validation result

After staging only this lane's explicit evidence paths, read-only validation
passed for both original and independent Proof records, all five original plus
five independent native correlations, and disjoint event identities (5 versus
5). Exact build command/receipt/toolchain, fixture and input-manifest identity
match; the independent environment validates against the original environment.
Review shape, independent session identity, original packet digest and replay
blob hash all passed. This uses `Tree(staged=True)`, `validate_proof`,
`validate_correlations`, `validate_environment`, `review_shape`, `read_blob` and
`packet_sha256`; it does not install a trust anchor or simulate an owner response.

Canonical review SHA-256 (owner_receipt excluded):
`b4c214907e43eb4ae7328a005c33e7558f215791928a77cc469edb167b7d1fba`.
Full accepted-review validation is not claimed: owner receipt is null and the
prospective disposition references are not authentic owner dispositions yet.
