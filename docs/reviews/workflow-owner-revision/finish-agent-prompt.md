# Prompt: finish the existing workflow repair, not Preferences

This is an operational handoff for `dat-workflow-owner-revision-71j`, not a
replacement roadmap, a new mechanism approval, or a claim of task admission.
Read `admission-owner-20260916.json` beside this file for the owner exception
and the reproduced installed-policy blocker. The issue remains intake until
the actual Frontier and owner-pinned coverage transaction is admitted.

Paste the following into the session holding the prepared workflow candidate:

> Resume the existing owner-directed workflow revision repair,
> dat-workflow-owner-revision-71j. This instruction does not ask you to implement
> or accept Preferences. Preserve /tmp/datum-gp-cm05-revision and its ownership.
>
> Read CLAUDE.md, AGENTS.md, and the committed admission record at
> docs/reviews/workflow-owner-revision/admission-owner-20260916.json. The owner
> has authorized governance-only admission despite the missing Frontier entry;
> do not ask for that same generic approval again. This is not approval of an
> unseen trust-promotion packet or a waiver of installed enforcement.
>
> Do not resume gate implementation or expensive proof before reconciling the
> start-work boundary. The admission diagnostic shows that adding a Frontier
> item without promoted coverage fails, and editing coverage under the existing
> authority also fails. Prepare the bounded governance-only admission and its
> exact owner-controlled policy-promotion handoff using unchanged installed gate
> code. Do not solve this by applying the whole candidate, relabeling the repair
> as an external lane, using --no-verify, or privately repinning trust. If an
> existing promotion mechanism cannot support admission, report that precise
> mechanism gap before writing another implementation. Never claim enrollment
> or a lease merely because a proposal contains them.
>
> After verified admission, use the actual canonical task details and synchronize
> its Frontier claim with br before further scoped edits. Keep the existing
> candidate as recoverable prior work, not proof of prior process compliance.
> Review its full diff and evidence routes; reconcile against current main by
> issue ID rather than copying its old tracker or roadmap snapshot. Reuse valid
> unchanged producer evidence with its original identities. Do not call the
> existing 81 tests independent review or installed integration proof.
>
> Ensure the admitted requirements cover the whole failure: legitimate
> owner-directed revision without product acceptance; a usable governed
> preparation/admission path; synchronized ownership before implementation;
> durable candidate preservation and substantive commits; and an honest
> completion handoff. Do not invent additional milestones conversationally.
> Temporary directories are not governance exemptions. Git hooks cannot prevent
> arbitrary off-repository editing; distinguish enforced tool entry points from
> agent obligations and test the enforcement actually claimed.
>
> Finish the admitted integration proof, independent review, exact owner-controlled
> promotion and installed verification. Resolve the existing environment-selection
> parity defect through its owning workflow route, dat-wdq-environment-parity-jmo;
> do not blindly refresh digests. Preserve product, prototype, dependency, source
> permission, and final acceptance boundaries. Commit only this lane's owned
> changes with Problem, Change, Proof and Roadmap explanations.
>
> Do not stop at "candidate tested" or mark the repair complete until its
> installed behavior and supported product-owner handoff have been verified.
> Report actual remaining work and any exact owner decision needed. Retain valid
> historical results without rerunning broad suites merely because time passed.
> Keep scratch space bounded and remove owned disposable artifacts after safe
> preservation. Do not recreate the earlier Documents-directory sprawl.
