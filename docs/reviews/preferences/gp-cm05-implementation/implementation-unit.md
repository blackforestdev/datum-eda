# GP-CM05E acceptance implementation unit

This is implementation evidence, not a ProductionReportV2, step completion,
owner acceptance, or permission to enter GP-CM05A. The exact approved promotion
and synchronized claim remain recorded in installed-authority.json. The original
/tmp/datum-gp-cm05-revision packet remains unchanged.

## Problem and delivered correction

Historical GP-CM05 checks located named tests and trusted aggregate summaries;
the acceptance runner could print PASS without full-workspace tests, strict
Clippy or the three missing GUI budgets. The adopted contract and V2 matrix now
require 389 case rows, 48 native scenarios and 90 measured variants, binding
actual outputs to candidate inputs, binaries, fixtures and display coordinates.
The checker derives trial verdicts from complete raw samples and retained state,
rejects stale independent review, and keeps owner acceptance separate. Synthetic
regression evidence can produce only synthetic_validated. Historical V1 evidence
is inspectable only as historical_incomplete.

The capture layer preserves failures, streams, timeout status and actual test
counts. The standalone release collector uses durable private fixture roots,
clears legacy Preferences/daemon overrides, retains monotonic timing and wait4
RSS, and measures mutation acknowledgement from automatic foreground-TTY APPLY.
Its incomplete outputs are explicitly partial_diagnostic. The bounded Rust
proof runner includes gui-render and refuses zero matching tests.

## Independent implementation review

The independent session /root/admission_review inspected the validator and
contract, replayed the complete 30-test suite successfully, and then replayed
11 focused measurement tests successfully after the final two corrections.
It reported no remaining blocking findings in this implementation unit.
This is not independent production replay or a native usability review.

Resolved findings include wrong-type matrix data; unbound executable roles;
reused raw trials/native captures; unretained or invented storage totals;
arbitrary baseline labels; stale review for different evidence under one commit;
malformed replay crashes; repeated trials; untyped warm-ups; discontinuous
mutation history; inherited owner legacy-state paths; and overly broad warm-up
budget exemptions. Warm-up latency alone is exempt; RSS/storage remain bounded.

Every member of the owning route (59 sources and 20 existing consumers, plus
the adopted contract) was reviewed for Preferences boundary-bearing content.
This does not claim fresh exhaustive review of unrelated Revision/Publish
mechanisms or rendered prototype conformance. No protected HTML was edited.
Historical aggregate seed proposals remain subordinate to PM037/PM040.

## Verification and remaining work

Matrix, historical-evidence disposition, Preferences boundary, dependency,
Cargo-resource, source-health, spec governance, parity, traceability and progress
checks passed. Project-state checks are run with their required temporary Git
index; Python proof commands suppress generated bytecode in the shared checkout.
The installed commit gate remains enabled.

Real release-binary diagnostic capture is preserved at
/tmp/datum-gp-cm05-raw-diagnostic-20260916-3/capture.json: three trials,
one measured sample plus ten warm-ups per variant, 69 variant/trial rows.
This tests collection behavior, not 100-sample budgets or same-candidate release
readiness. Earlier failed/smaller captures remain retained rather than overwritten.
A separate final Project capture exercises nondefault imperial Units seeding at
/tmp/datum-gp-cm05-project-capture-final/capture.json.

GP-CM05E remains in progress. Required remaining work includes actual complete
case/fixture capture, continuous generation storage accounting, reachable-daemon
mutation capture, all native Wayland/X11/AT-SPI/screen-reader and resource proofs,
complete guarded workspace/Clippy closure for the final committed product,
independent production replay, release/support evidence and GP-CM05A owner review.

The audit captured dat-preferences-cli-daemon-routing-i4d: ordinary Preferences
CLI requests currently open a standalone service; only AuthorizeMcp dispatches
to the owning daemon. The existing product-surface contract requires discovered
owner routing and forbids fallback after a reachable daemon refusal. The issue
is related to dat-global-preferences-completion-f84 and remains open. This unit
does not claim that runtime correction or close the task.

No dependency or licensing change, installed-trust change, workflow-repair work,
prototype edit, reserved descriptor activation, live Project following, broader
Project Preferences, Publish or Revision authority is introduced.
