# Independent infrastructure proof review — publication review pending

Reviewer: `wdq-independent-review-20260906`, task `/root/wdq_independent_review`.
Date: 2026-09-09 UTC. This session did not author the rollout implementation or
its original producer evidence. It authored only independent observations and
review artifacts; it did not change production, prototypes, shared tracker,
Frontier, installed trust, or main commits.

## Disposition and exact boundary

The corrected infrastructure proof packet
`bf726cbb753c7e3d3956f7a624ee0286268a78d90fd2d5e5e17b9b744317295e`
has passing independent INFRA-S01 through INFRA-S06 replay. The typed approval
is limited to that proof and its exact reviewed inputs. Overall WDQ-REVIEW
completion remains pending actual full-candidate promotion inspection by both
producer and reviewer, followed by the final publication-delta review. This
document does not approve activation, complete WDQ-I04, or grant owner defect
dispositions. The typed owner receipt and all five disposition references are
null. The earlier two-finding provisional review is retained separately; it is
historical, not the final complete issue inventory.

Original evidence candidate:
`69583d27a092465eddc34c77677d1c044fb2c28a`.
Corrected evidence candidate:
`227ee2f4306d10806722fbae77cff3cfe9e5fc32`.
Actual unchanged runtime source:
`9cab72daab46122da6d2c45ab7f972814f702880` in the retained
`wdq-i02-assembled-20260908/candidate` checkout.
The original producer packet `857638f21ee465d72f0697d6ef698a4206cd91a6af1f2297d604baf6f2223e63`
is historical and insufficient on its own; the corrected packet supersedes two
Rust-hook observations and adds their named-tool evidence without changing the
selected environment, source inventory or fixture identity.

## Actual independent execution

The final matrix contains 1,123 outer process invocations: 608 primary and 515
fresh recovery invocations, covering every required variant and all applicable
staged CLI, exact-candidate CLI, selector check, selector details and pinned
owner-hook surfaces. There are 4,412 distinct raw observer event IDs in the final
selection, with no producer overlap. These are newly executed processes, not
copied producer events or assertions. Existing case evaluators checked exact
diagnostic codes, paths and causes, false acceptance/readiness flags, actual
handler entry, and protected before/after snapshots. They do not claim complete
descendant tracing. All final selected captures pass those checks.

| Scenario | Primary | Fresh recovery |
| --- | ---: | ---: |
| INFRA-S01 | 23 | 18 |
| INFRA-S02 | 145 | 135 |
| INFRA-S03 | 110 | 75 |
| INFRA-S04 | 100 | 80 |
| INFRA-S05 | 36 | 23 |
| INFRA-S06 | 194 | 184 |

The five INFRA-S04-11 real-roadmap invocations restore the retained real
`source.bundle`, including the accepted pilot and actual 56-row roadmap, rather
than replacing it with a small synthetic fixture. The hostile case families
remain synthetic INPUTS to actual production validator processes, never native
EDA OUTPUT or owner approval. Five real owned-process interruptions preserve
terminal status and state, with fresh successful recovery runs.

The exact original build command was independently rerun using Python
`-I -S -B`: 152 source inputs matched revision and worktree, 140 Python modules
compiled in memory, zero output source writes, exit zero. Input manifest:
`ab90c27b89f820df22200fc8ed1c200faae5536b7a28d64b14ae25869fc03564`.
Interpreter SHA-256:
`17b78e0a93175e86f9ac03141924fd7a7f0c0c52e66b34bfa0de20ffef989df1`.
Exact argv, environment, timestamps, PID, raw stdout/stderr and source/function
hash observations remain in the archives. Separate real execution of the 44
existing activation/preflight/state tests passed; those disposable synthetic
publication tests are supplemental, not full-candidate or installed activation.

## Findings and retained attempts

`dat-wdq-replay-object-identity-6zl` — blocking evidence defect, owner disposition
pending. The first recipe replay passed behavior assertions but regenerated Git
objects: 56 HEADs and one additional nonancestor-base ref differed. It was not
byte-identical replay and is retained as recipe-behavior evidence only. The
reviewer then copied the original post-setup fixture objects into fresh owned
repositories, compared files/modes, index entries, HEAD, refs, symbolic HEAD and
exclusions, and reran the affected cases through the existing capture API.
All 57 corrected cases pass the case-specific evaluations. Exact copied input
repositories, including their Git objects, are archived. No original fixture,
clock or lease was changed; live-claim corrections completed while genuinely
valid. No expired lease was turned into a success.

Three clone-setup failures are preserved: two selector calls refreshed only
copied index stat-cache metadata, and one hook refused a copied absolute runner
location. Subsequent owned setup refreshed index metadata before capture and
relocated only fixture-local hook/runner paths; semantic input identities were
rechecked and all three fresh captures preserved the complete observed state.
The corrected text-R case was repeated with the exact explicit no-color
environment. A Rust collector keyword-argument error launched no child and is
retained as an unsuccessful collection attempt, not matrix evidence.

`dat-wdq-formatter-evidence-7wm` — blocking evidence defect, owner disposition
pending. The historic observer-only preflight plus raw PATH did not bind invoked
rustfmt bytes to the original H captures. The producer supplied new primary and
recovery H observations with contemporaneous before/after path/hash/version
receipts. The reviewer independently repeated both and compared complete
semantic inputs. Both pass. Independently observed rustfmt before/after SHA:
`c7d4d911e75fa66321fd04ac81cf6ea1b3a2c759efe39e61caccb10f3c850918`.
Those receipts are supplemental typed state/tool evidence as permitted by the
bounded contract; they do not alter the closed owner-selected environment or
pretend its original tool list contained rustfmt. The corrected producer packet
and independent replay retain this finding rather than hiding the first gap.

## Scope, authority and inapplicability

Three pre-existing relevant obligations are also included, not silently dropped:

- `dat-wdq-environment-scope-kgq`: S06 independently demonstrates the exact
  per-enrollment mixed/headless selection and all required refusals without
  changing the accepted pilot. Proposed PM042 ratification and installation
  remain owner-only; this is candidate repair evidence, not live completion.
- `dat-wdq-git-replacement-authority-2cm`: inspected pinned Git readers disable
  replacement/graft and repository redirection while retaining the explicitly
  selected index. An additional 23 independent existing index/support/hook
  regressions pass, including actual alternate-index CLI and pinned hook paths.
  Candidate repair does not imply that the old installed runner changed.
- `dat-wdq-promotion-delta-mb9`: the reviewed full-history publication path
  preserves ordinary execution-claim enforcement rather than substituting an
  empty delta. The 44 supplemental activation tests pass, but the actual complete
  publication inspection described below remains necessary. No owner resolution
  or live activation is inferred from the implementation or those tests.

These retain blocking accounting with null references pending exact resolution
and owner disposition; no tracker closure was performed by the reviewer.

Controlling AGENTS.md and CLAUDE.md were read. The complete owning
`workflow-delivery-infrastructure` route and the source/consumer documents of
`workflow-delivery-rollout` were reviewed, including PM042, the infrastructure
contract and case matrix, rollout plan and execution requirements, all seven
clause inventories and completion records, mappings, classifications, exact
coverage/source permission proposal and external Preferences boundary. Runtime
code does not override these documents. The proposed 141 exact permission
paths grant no prototype or Rust product edits; 152 read inputs are not write
authority. A static local-import inventory found no repository Python import
outside the declared closure. Hook/bootstrap paths, prerequisites, selected
environment and data inputs were inspected separately from interpreter hashes.

All scenario dimension dispositions were reviewed. Precision and undo/redo are
N/A in all six groups: no CAD number, geometry or design mutation exists here.
Cancel and save/reopen are N/A only in S01-S05 because S06 requires actual
interruption/nonmutation and fresh-process recovery; no native project Save is
claimed. Accessibility is required and exercised as usable noninteractive
no-color text in S01, and N/A in S02-S06 because S01 owns that obligation, not
because diagnostics can be omitted. No GUI/AT-SPI, CAD units, snap, connectivity,
selection identity or Global Preferences acceptance follows from these checks.
History-only R cases do not certify selector history; staged/worktree divergence
cases retain their specified distinct surfaces. No entire group was waived.

Bounded named interpreter, Git, shell, env, realpath and invoked rustfmt paths,
hashes and versions are retained. Python startup/user-site injection was
disabled; isolated HOME/XDG_CONFIG_HOME and Git configuration were used. This
is not hermetic runtime completeness: exhaustive descendant/shared-library
tracing and arbitrary host-library substitution detection were not performed.
Full drift/CI, Rust/GUI builds, native adoption cohorts, installed-main validation
and owner-controlled activation remain separate. GP-CM05V and the existing
accepted pilot remain unchanged.

## Remaining publication obligation

The retained evidence candidate is not an activatable main publication. An
isolated proposed claim-free WDQ-I04 base/candidate may be inspected without
changing live REVIEW status. Its actual complete publication delta, prepared
runner pins, exact caller-selected inspection response and successful full
inspection must be independently verified before overall review completion.
After that evidence is recorded, final main handoff changes require one last
exact delta inspection. Neither a synthetic success nor this provisional
matrix-proof approval substitutes for those observations.
