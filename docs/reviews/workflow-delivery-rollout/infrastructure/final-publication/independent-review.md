# Final publication inspection — activation held

Reviewer: `wdq-independent-review-20260906`, independent of implementation and
original production proof. This bounded review made no shared source, tracker,
Frontier, installed trust, hook or owner-file changes and performed no activation.

## Exact candidate checks

Actual main base: `7b52d57f4a4c9b42211fffc0ea62df5d2355933e`.
Candidate: `2d60cd05dca71727ba28b1355b988de653eed82f`.
The candidate is one direct-parent commit with 131 touched paths. Independently
rebuilt full-delta SHA-256:
`ff2f01bc1d9ba0bd8c29a0538bcfdfef8a76faf88e0235afaa2f4b6c1cf4a259`.

All 152 original tested source inputs match `9cab72daab46122da6d2c45ab7f972814f702880`.
All other 55 Frontier lanes are unchanged. The base and candidate select actual
claim-free WDQ-I04; no hypothetical completion record substitutes for main.
The five closed findings and their unique governed owner disposition references
pass the existing disposition validator. Compared with the retained original
independent review, only those five reference values changed; reviewer identity,
verdict, replay, findings and null owner receipt remain intact. The owner
explicitly authorized candidate-repair closure while retaining promotion and
installed verification under WDQ-I04. No new independent approval was fabricated.

Current owner-disposed review file SHA-256:
`0b7fc8d5fba700869f4467e00941b4c747caeb30521b697b1221f7daefe42201`.
Canonical review digest:
`153a2b7c140bd83ba4c6fb31706607322606b363f48eaa39a97b5b032a971168`.
Original producer/replay identities remain unchanged.

## Actual final full inspection

A fresh isolated clone independently executed the authenticated candidate
`workflow_delivery_preflight_cli.py --inspect-promotion` with Python `-I -S -B`,
explicit base/candidate, separately prepared support, and a newly selected
synthetic inspection-only response. Exact argv, PID, times, stdout/stderr,
request/response provenance and before/after states are retained locally.

Request SHA-256:
`b09d43f8cb233d2bb8f37ffee4c31d0cb297a7589ee7a0e03ee889c44eb45e98`.
Successful full-log SHA-256:
`b341a012a6cddc4512274d071abb615c56e9067f559999c607caa9783275ed14`.
The process exited zero with no findings. Full protected state and prepared
support were unchanged; `publication_authorized` and `activation_asserted`
remain false. This proves isolated candidate inspection, not actual-main clean
inputs, owner approval, installed verification or activation readiness.

## New blocking compatibility finding

`dat-wdq-workspace-inputs-zmt` remains unresolved. Main contains 116 ignored
script paths, including Python caches and the owner's `scripts/run_gui_doa2526.sh`.
They were not deleted, relocated, modified, executed or silently omitted to
manufacture a clean-input result. Local Beads runtime state also must not be
silently removed merely because a broad preflight root includes it.

Three fresh isolated selector processes used the exact final candidate and
candidate-selected trust, with unchanged original source:

| Isolated input | Actual result |
| --- | --- |
| Clean candidate | Exit 0; `frontier_count: 56`, `ok: true` |
| Only ignored `scripts/run_gui_doa2526.sh` added | Exit 1; `WDQ-COVERAGE` identifies that path |
| Only ignored `scripts/__pycache__/agent_mcp_adapter_probe.cpython-313.pyc` added | Exit 1; `WDQ-COVERAGE` identifies that path |

Both refusals give the precise cause: production change has no promoted scope
with synchronized live execution claim and required enrollment. The added files
were exact copies into owned fixtures, not edits to the originals. Each actual
process preserved its complete before/after protected state. Input hashes and
fresh raw observations are retained in `selector-compatibility/`; this is
isolated installed-selector simulation, not actual activation or source repair.

The cause is explicit: the selector calls coverage validation, whose worktree
transaction enumerates ignored/untracked paths under policy production roots
using `git ls-files --others` without `--exclude-standard`. Those roots include
`scripts`. Exact-file activation preflight roots therefore cannot cure the
subsequent installed-selector refusal. Omitting the caches/user script only from
preflight would conceal the compatibility problem until after mutation.

## Recommendation and boundary

Do not activate this packet. Preserve the five legitimate owner resolutions
and all passing proof, but keep the newly discovered compatibility issue open.
The owner should authorize a bounded compatibility decision/repair covering
derived runtime artifacts and the separately owner-authored ignored script.
Any relocation of the owner script needs explicit permission and preservation;
cache handling needs a durable policy or placement decision, not an unreviewed
cleanup that will fail when caches recur. No policy narrowing, source fix,
deletion or alternative mutation path is authorized by this preparation task.

After an authorized remedy, repeat the targeted clean/ignored compatibility
checks and the exact actual-main input/trust preflight and publication inspection.
Recording this new issue changes main, so this candidate/base pairing must not
be advertised as current activation-ready authority afterward. Reassemble and
review the resulting exact final packet; renewed applicable proof is required
if its relevant source/authority inputs change. Actual activation still requires
separate exact owner approval, coordinated writer pause and installed checks.
