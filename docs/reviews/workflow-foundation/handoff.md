# Foundation contract: owner and consuming-lane handoff

Status: WDQ-F03 owner disposition recorded; contract/handoff adopted, not implementation approval.
Tracking: FOUNDATION-WORKFLOW-SPEC / `dat-manual-foundation-contracts-fsw`.
Owning route: `foundation-manual-workflow`.
Contract: `specs/FOUNDATION_MANUAL_WORKFLOW_CONTRACT.md`.
Research: `research/process-quality/FOUNDATION_MANUAL_WORKFLOW_RESEARCH.md`.
Authority review: `docs/reviews/workflow-foundation/authority-review.md`.

## What this packet does and does not accept

<!-- EVIDENCE:FOUNDATION-WORKFLOW-SPEC:WDQ-F03-APPROVED -->
### Recorded owner disposition

The owner replied **“approved /goal resume”** to the four-request WDQ-F03
packet. This records approval of CONTRACT, OPEN-CHOICES, PREREQUISITES and
ROLLOUT with precisely the limits printed in that packet. The reviewed baseline
is `b429820e`; before this disposition, the handoff SHA-256 was
`afdc7514c718e7daa6397ffbd8082f67dd25dbacfd0dea5e67aa500c6582dd6a`
and the concrete contract SHA-256 was
`6b5cfd07a13066cbcaa6495b1e7d07f42d784609fb610491b97fc0a35a0c5fa5`.
These identify the presented files; they are not a fabricated PM041 receipt.

- CONTRACT: adopted bounded inherited requirements and concrete handoffs;
  unresolved product choices remain unapproved.
- OPEN-CHOICES: authorized scheduling and research/specification follow-ups,
  not the writer/gesture mechanisms, fixture budgets or their implementation.
- PREREQUISITES: authorized exactly the three foundation edges in the table
  below. This governance transaction records them; all previous edges remain.
- ROLLOUT: authorized preparation of the broader enrollment, tripwire and
  three-slice adoption packet, not trusted gate promotion or product execution.

The original proposed/decision-request text below is retained as the reviewed
packet. Its references to pending owner disposition are superseded only by
these four bounded approvals. Broader rollout, owning-lane product reconciliation
and every unresolved mechanism remain unfinished. Preferences ownership and
GP-CM05V, pilot authority and local trust are untouched.

The packet connects a normal native launch/create/open doorway to exact board
editing and durable reopen, with a local-library/electrical-to-physical handoff.
It carries concrete F1–F8 examples rather than another general capability audit.
Inherited requirements are distinguished from proposed design choices. No
prototype, runtime, fixture, native proof or consumer delivery is approved by
writing or checking this packet. GP-CM05V remains the separate Preferences lane.

<!-- EVIDENCE:FOUNDATION-WORKFLOW-SPEC:WDQ-F02-COMPLETE -->
WDQ-F02 authorship is complete: the contract covers exact values and refusals,
selection/snap, direct/proposal and persistence timelines, pinned bindings and
two-Project settings isolation; primary writer/durability research and bounded
visual/consumer handoffs are included. Unresolved product choices below are
explicit execution blockers, not silently selected answers. The next decision
belongs to the owner under WDQ-F03. This does not close the broader rollout goal.

## F1–F8 consumer delivery map

| Concern | Required output from this contract | Accountable existing lane / handoff |
| --- | --- | --- |
| F1 Units | Exact canonical/display/context table, no geometry rescale | PROJECT-PREFERENCES-SPEC consumes accepted Units authority; no change to GLOBAL-PREFERENCES-COMPLETION or its acceptance |
| F2 Entry | Correctable partial input, exact vectors and signed bounds, lossless focus/no-op/cancel | NATIVE-AUTHORING / NA-C01 binds each field to the existing shared service, not a private parser |
| F3 Selection | Stable target, granularity, focus, lifecycle and compound refusal | UVT-S5A-BUILD / S5A-C01 remains read-only; NA-C01 owns later mutation use |
| F4 Frames/snap | View/model separation, exact entry, grid/object precedence | S5A-C01 reviews shared backbone; NA-C01 specifies authoring consumers without reviving old grids |
| F5 History | Correlated typed action, one committed batch, no-write cancellation, compensating undo | GUI-WRITE-PATH / GWP-C01 and NA-C01; existing writer decision remains prerequisite |
| F6 Doorway/recovery | Ordinary native launch/create/open, collision/second-writer/refusal and recovery matrix | `dat-native-project-startup-vrf` is the dedicated intake; GWP owns shared write authority, not a second Project store |
| F7 Binding/connectivity | Pinned local part, explicit PinPadMap and ComponentInstance, formation versus identity | GUI-SURFACE-SPECS / SURFACE-S01 and NA-C01; ambiguous wire/junction behavior stays blocked pending disposition |
| F8 Settings | Global future-seed versus current Project versus Publish, immutable Reset receipt | PROJECT-PREFERENCES-SPEC and native consumers; no new descriptor activation or Preferences rewrite |

The three repaired implementation consumers are unassigned and unclaimed at
`2fee7440`; their named selectors pass. These are routed responsibilities, not
evidence of acceptance by a currently absent implementation owner. Each future
owner must review this packet with the current route, claim its own scope and
reach its own execution/acceptance boundary. No parallel claim is inferred.

## Bounded handoff to Claude — design lane only

This is a return list, not a claim that messages were delivered to another
terminal or that Claude has accepted work. Do not edit any HTML from this lane.
Before authoring, Claude must confirm its active file claim and obtain the
applicable owner disposition; preserve the accepted Global Preferences work.

| Exact file and region | Required outcome | Decisions to preserve / proof expected |
| --- | --- | --- |
| `docs/gui/prototypes/board-editor.html`, `#insp`, Position X / Position Y / Rotation rows and the status-bar Grid segment | Provide an exact-entry state sequence for rounded display, exact focus text, incomplete/invalid text, unchanged Enter, Escape, successful commit and stale/locked refusal. Use the contract's 5080001 nm rounding example; do not merely replace the existing static coordinates. | PM026 selected identity and focused-pane authority; accepted Units precision and nm exactness; existing component hierarchy/tokens. Review DOM/CSS plus matching rendered states, narrow layout, focus and non-color error indication. Static HTML is visual evidence, not native journal proof. |
| `docs/gui/prototypes/project-preferences-category-study.html`, P-B ordinary Units, P-G refusals, P-H receipts, P-I keyboard, P-J accessibility | Reconcile only the Project Working Units consumer examples needed by F1/F8, including immutable Reset target, typed unavailable receipt and A/B scope. Preserve P-A…P-J as an open Project-owned target until its owner accepts it. | PM039/040, eight separate selectors and paired-but-independent unit/precision; immediate journaled Project setting changes, no geometry rescale, no Navigator route. Prove keyboard/non-color states and metadata; do not copy Global UNIT acceptance into Project approval. |
| `docs/gui/prototypes/units-and-grid-model.html`, section containing “Two objects in a Datum schematic are electrically connected if and only if their integer coordinates are identical” and the “one requirement” pin-snap callout | Return the formation-versus-identity ambiguity for the proposed connectivity decision. After disposition, distinguish pin/endpoint attachment, T junction, unjoined crossing and explicit joined crossing; remove any implication that names or grid coincidence alone define persistent NetId. | Preserve accepted metric grid defaults and PM003/026 stable identity. No new connectivity mechanism may be ratified by an HTML edit. Require the owning numbered decision plus matched scenario diagrams before authority refresh. |
| New proposed `docs/gui/prototypes/native-project-doorway.html` (does not exist in this packet), named New/Open/Cancel/error/recovery states | After owner authorizes design, provide the ordinary no-argument doorway, New preview and absent-destination publication, open failure, second-writer state and diagnostic recovery without a mandatory formal Revision ceremony. | One cohesive native application, optional AI, PM018 genesis, PM038 Unmanaged default and explicit recovery truth. Include actual focus order, accessible names/reasons and cancel destinations. No always-on-top workaround; input modality must be tied to its owning window. |

No change is requested to `preferences-window.html`, Global Preferences
acceptance evidence, or its current production implementation. A necessary
cross-route change must be returned to that owner as a new bounded request.
Claude's completed handoff must name its commit, exact regions and captures;
the implementation owner later supplies matched native evidence. Neither may
refresh the other's approval or fabricate a visual-truth commit marker.

## Proposed prerequisite transaction — not applied

<!-- OWNER:FOUNDATION-WORKFLOW-SPEC:WDQ-F03:PREREQUISITES -->
The recommendation is to add the completed foundation contract as an explicit
prerequisite to the three named consumers, preserving every existing edge:

| Frontier / bead | Existing hard prerequisites | Proposed addition |
| --- | --- | --- |
| GUI-SURFACE-SPECS / `dat-gui-surface-specs-usb` | None | `dat-manual-foundation-contracts-fsw` |
| GUI-WRITE-PATH / `dat-gui-write-path-qiu` | `dat-marking-menu-shell-g0w`, `dat-project-write-ownership-lock-0ne` | `dat-manual-foundation-contracts-fsw` |
| NATIVE-AUTHORING / `dat-native-authoring-depth-sf9` | `dat-gui-p2-cross-probe-27z`, `dat-gui-surface-specs-usb`, `dat-gui-write-path-qiu` | `dat-manual-foundation-contracts-fsw` |

Apply only after explicit owner approval, with synchronized beads edges,
Frontier dependencies, foundation post-completion successor IDs and regenerated
Progress. Do not change canonical selection or execution authorization in that
transaction. Validate the complete graph before landing; this table does not
permit dropping an inconvenient blocker.

The doorway intake already depends on the foundation contract. Promote its
planning contract separately after disposition; its relationship to native
authoring is currently nonblocking and must not be silently turned into a
requirement to finish all native authoring before creating a doorway. Conversely,
native delivery must demonstrate the actual doorway, not waive it because the
engine can open a path. Specify planning prerequisites versus acceptance proof
separately to avoid the earlier “proof requires an unbuilt surface” cycle.

No dependency change is proposed for the active Preferences lane or
PROJECT-PREFERENCES-SPEC. No current consumer is selected, claimed or executed.

## Ordered owner decision packet

<!-- OWNER:FOUNDATION-WORKFLOW-SPEC:WDQ-F03:CONTRACT -->
### 1. Adopt the bounded contract and its limits

Recommendation: approve the inherited F1–F8 requirements, exact-value examples
and handoff obligations as the foundation contract. This adopts neither the
unresolved mechanisms below nor execution or product acceptance. A revision
request should identify the clause/example and expected correction.

<!-- OWNER:FOUNDATION-WORKFLOW-SPEC:WDQ-F03:OPEN-CHOICES -->
### 2. Keep unresolved product choices blocked and schedule their resolution

Recommendation: authorize the following bounded specification follow-ups,
not mechanism implementation or acceptance by this packet:

- **Project writer ownership:** `dat-project-write-ownership-lock-0ne` must
  produce a numbered decision. Preferred candidate is the shared engine guard
  used by all writers/recovery; daemon-only ownership remains the alternative.
  Resolve lock-target stability, aliases, inheritance, crash and filesystem
  support. The research supplies the operating-system constraints.
- **Wire gesture/formation:** use `dat-grid-connectivity-authority-5s6` for
  owning-route reconciliation and a numbered decision where needed. Proposed
  gesture is one preview chain, explicit Finish commits, Escape cancels. Proposed
  formation distinguishes exact pin attachment, endpoint-on-segment T with an
  explicit authored join, crossing without join, and explicitly joined crossing.
  Existing NetId rename/split/merge identity remains controlling. These proposed
  native gestures are not ratified by accepting inherited Units requirements.
- **Doorway visual and native fixture:** `dat-native-project-startup-vrf`
  carries design/fixture selection into a scheduled planning packet. No bundled
  library, imported fixture or CLI-only doorway is silently substituted.
- **Performance:** PM012 still needs fixture-backed budgets. Proposed first
  local-fixture targets for review are p95 preview within one 60-Hz frame and
  p95 committed edit within 100 ms; these are design proposals, not measurements
  or evidence of feasibility. The packet must define sample count, warm/cold
  conditions, hardware, scale and inclusion of durable I/O before ratification.

Unresolved required cases remain unavailable for implementation acceptance.
Do not close their issues merely because this document names them. Research
and specification are real roadmap work with owners and reviewed outputs.

### 3. Approve or revise the exact prerequisite additions

Use the PREREQUISITES table above. Approval means those three additions only,
not arbitrary reordering, new dependencies or a successor execution grant.

<!-- OWNER:FOUNDATION-WORKFLOW-SPEC:WDQ-F03:ROLLOUT -->
### 4. Preserve the unfinished broader rollout obligation

After foundation disposition, the workflow-quality lane still owes explicit
delivery enrollment/trust proposals, refusal tests and measured adoption on
three separately enrolled slices beyond the pilot. The repaired S5A, write-path
and native-authoring plans are consumers, not proof of enrollment or delivery.
Do not make this lane implement those product features to finish process work.
Coordinate their actual implementation owners, observe their proof/review
transitions and collect adoption evidence without taking over their tasks.

Recommendation: retain this broader goal and authorize preparation of its
bounded rollout/adoption packet only. Actual trusted gate changes and enrollment
require the exact PM041 owner-controlled promotion; no receipt is implied here.

## Required recorded response

For each numbered request, reply approve, revise with the concrete correction,
or defer with the reason. Approval of request 1 does not approve requests 2–4.
Use the exact response format printed by the fresh WDQ-F03 selector. A general
“proceed” must not silently settle the unresolved product choices.
