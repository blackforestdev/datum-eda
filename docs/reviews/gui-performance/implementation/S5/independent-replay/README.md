# Independent S5 replay inputs

This packet pins the current candidate, preserved S5 comparison baseline, native
fixture, governing acceptance rows and available commands. **Final replay is not
ready.** Known performance failures and incomplete methods remain listed in
`packet.json`. No requirement passes by preparing this packet.

Use the existing adoption map and PM045/PM047 contracts as authority. The frozen
225-row `requirements.json` is a review input, not another roadmap or acceptance
framework. Preserve all S0–S4 implementation exits and every carried qualification
predicate. The source snapshot includes the existing 14-consumer indexes.

The candidate binary is `b8c1fa1b…`, produced by the shared X11 owner-focus repair
and restored after the rejected quad-shader experiment. Its current source is
bound in `source-bindings.json`. Preserved executables are on the local project
disk in `target/pm045-independent-replay-artifacts/`; hashes and source/evidence
identifiers are in `packet.json`. These ignored binaries are not transported by
Git. A reviewer on another machine must reproduce and pin its actual binaries;
a historical hash must never be asserted for a fresh build without verification.

The available baseline `c425e089…` precedes the S5 final-resolve change. It supports
that bounded S5 comparison, not a whole-PM045 improvement claim. The original S0
baseline commit and historical binary hash are separately recorded; availability
of that old executable is not established. No formal relative claim is selected.

`fixture.tar.gz` contains the exact native partitions, complete journal, revision
history/blobs, import map and check records, plus the archived source board.
Only generated terminal context/session data is excluded, as enumerated in
`fixture.json`. Never reconstruct the native fixture by reimporting a mutable
board: preserve all object IDs and painter ordering. Each endurance invocation
copies the fixture into its own output directory before launching Datum.

Run the exact prerequisite and available method commands from `commands.json`,
from the repository root. The window-resource driver prints its fresh output
directory. It records first-use separately, warms all hosts, then performs a
60-minute round-robin run with 200 cycles per auxiliary host. This is one
**diagnostics-on window/resource method**, not the complete final qualification
campaign. Independent method review identified fixture and failure-preservation
defects and verified their corrections; parser tests and completed cycles do not substitute for all resource
caps, recovery injection, other workloads or the prescribed production negatives.

The full required schedule remains MET-01/MEM-03/STAT-01: both native backends,
supported 1x/1.5x/2x rows at 60Hz, three warm trials, ten cold starts, explicit
cycle counts, complete counters and native output, 60-minute endurance with
200 cycles and 20 injected recoveries, and all original positive/negative
predicates. A missing executable method is listed as missing, not replaced by a
launch command or a shorter diagnostic. Diagnostics-off resource qualification
and matched diagnostics-on structural observation remain separate.

The owner authorized a fresh independent reviewer session. The reviewer must
inspect the methods and actually rerun the required trials against the stable
final candidate. Its current assignment is method readiness only. It must report
defects to the implementation owner and remain independent of implementation.
Do not replay each intermediate candidate. Immediately before final replay,
coordinate exclusive desktop and build-resource use, refresh candidate/method
pins, and record the reviewer's exact environment. No compilation or competing
native automation may overlap resource trials.

Record reviewer raw results in the existing S5 tree at
`independent-replay/results/<session>/<backend>/<scale>/<workload>/<attempt>/`.
Use fresh directories and retain failures, interrupted attempts and uncertainty.
Missing counters, overruns, dropped input and forced process cleanup cannot pass.
Keep GEFN, 8xAA/painter order, upload-before-acquire, dependency authority and the
Claude-owned prototype lane intact. Resize resource/temporal, displayed-frame,
T2 and unadmitted schematic exclusions remain explicit and unqualified.

Independent replay does not replace the owner's separate native UX/product
acceptance. The packet records no such acceptance and does not advance S5.

`tools/portal_pointer.py` prepares the required fractional pointer schedule using
the existing input-only portal API. Its offline schedule checks pass, but native
execution is pending desktop availability and fresh portal consent. See
`pointer-method-preparation.json`; this does not rehabilitate the integer XTest
trials, replace semantic/output evidence or satisfy final replay.
